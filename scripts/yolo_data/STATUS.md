# 95598 验证码训练数据状态记录

## 当前进度（2026-08-25）
- 有效样本：**6 个**（共 15 个标注目标）
- 目标：15-20 个样本后开始训练 YOLOv8-n

| 样本 | 目标数 | 标记位置 (页面坐标) |
|---|---|---|
| sample_000 | 2 | (566,475) (689,474) |
| sample_001 | 3 | (535,352) (612,402) (531,516) |
| sample_002 | 3 | (769,318) (532,483) (688,322) |
| sample_003 | 2 | (549,440) (683,317) |
| sample_004 | 3 | (697,511) (537,439) (616,362) |
| sample_005 | 2 | (652,356) (655,321) |

## 文件格式
- `sample_NNN.jpg`：合成图（上方答案条 3x 放大 + 下方大图 330x236），宽330 高(75+236)
- `sample_NNN.txt`：YOLO 标签 `class cx cy w h`（归一化，class=0 target）
- `sample_NNN.json`：元数据（标记页面坐标、点击记录）

## 注意事项（重要！）
1. **账号风控敏感**：95598 登录/点击次数太频繁会触发风控（已触发 2 次）
   - 每轮之间至少间隔 **90 秒**（工具 `--delay 90`）
   - 每次会话最多收集 **3-5 个样本**，然后停止
   - 若遇到风控：等待 24 小时解封后再继续
2. **数据收集工具**：`scripts/sgcc_annotate.py --rounds N --delay 90`
   - 已有样本会自动续编号（不覆盖）
   - 用户点击 → 工具自动检测 turing 标记 ①②③ → 保存样本
3. 样本少于 2 个标记的轮次会被自动跳过（数据质量保护）

## 明天继续
1. 等账号解封（风控 24h）
2. 运行：`python scripts/sgcc_annotate.py --rounds 5 --delay 90`（每天 3-5 个即可）
3. 凑到 15+ 样本后训练：
   ```
   conda run -n yolo python train.py   # 用 ultralytics + MPS GPU
   ```
4. 训练完构建 Playwright+YOLO 求解器 → 部署到服务器 Docker
